# 去除屋檐底部横线设计

## 目标

去除德宁之殿上下两层屋顶正面和背面檐口最下方的绿色横向圆线，使正背面檐口与侧面保持一致。

## 范围

- 删除 `createRoofLevel` 中为每层屋顶生成的正面 `eaveBand` 和背面 `rearBand`。
- 保留瓦面、瓦垄线、正脊、四条斜脊、脊兽、屋顶尺寸与材质。
- 不为左右侧面新增替代线条，也不调整摄像机或光照。

## 验收

- 自动测试确认每层屋顶不再包含檐口横向圆柱网格。
- 正面与鸟瞰浏览器画面中，屋顶最下沿不再出现独立绿色横线。
- 全量测试、类型检查和生产构建通过。
